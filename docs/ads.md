# 광고 — parkinon.com 웹

작성 2026-09-02. **광고 관련 값은 전부 여기 모은다.** 코드에 흩뜨리지 않기 위해서다.
코드 쪽 정본은 `site/src/lib/ads.ts`.

## 어디에 무엇이 붙어 있나

| 언어 | 네트워크 | 이유 |
|---|---|---|
| **한국어(`/ko/`)** | 카카오 애드핏 | 국내 광고주 네트워크 |
| **그 밖 (en·ja·fr·es·pt)** | **Adcash** | 애드핏이 해외에서 안 채워진다 |

해외 글이 400편 가까이 되는데 수익이 0 이던 것을 메우려고 **2026-09-02 에 켰다**(오너 지시).

## 🚨 유럽에는 안 보낸다

**EU 27 + 아이슬란드·리히텐슈타인·노르웨이 + 영국 + 스위스** 에는 광고 스크립트를 **아예 안 받는다.**
동의창(CMP)도 EU 대리인도 없기 때문이다 — 이 저장소가 원래부터 그렇게 정해 뒀다
("광고를 켜기 전에 대리인과 CMP가 있어야 한다", `website-plan.md`).

- 판정은 **클라이언트**에서 한다. 정적 빌드라 빌드 시점엔 나라를 모른다.
  `/api/geo`(Cloudflare 의 `CF-IPCountry` 를 그대로 돌려주는 Function)를 부른 뒤 결정한다.
- **나라를 모르면 안 띄운다** — 유럽일 수도 있기 때문이다.
- 목록은 `site/src/lib/ads.ts` 의 `AD_BLOCKED_COUNTRIES`.
  **대리인(연 €420)을 두기로 하는 날 이 배열을 비우면 그대로 켜진다.**
- 실측(2026-09-02): FR·DE·GB·CH 광고 요청 **0건**, AR·US 는 **14건** 정상.

## Adcash 계정

| | |
|---|---|
| 계정 | `admin@ourmine.co.kr` · Publisher **1208684** (toolshere 와 같은 계정) |
| 사이트 | `https://parkinon.com` · Website **1647146** · 분류 `News` · 성인광고 **OFF** |
| 심사 | 2026-09-02 등록. 심사 대기(주황) 상태에서도 **광고는 실제로 채워졌다** |

카테고리에 Health 가 없어서 `News` 로 넣었다(소식·정보 기사 사이트라서).

## 존

| 용도 | zoneId | 크기 |
|---|---|---|
| 본문 칸 | `12091598` | Display 300×250 |
| PC 목록 상단 | `12091606` | Display 728×90 |
| PC 우측 세로 레일 | `12091614` | Display 160×600 |
| **Autotag** | `vbzjudlnvz` | 팝업언더·전면·인페이지푸시·비디오슬라이더 자동 순환 |

크기는 **한국어(애드핏) 자리와 일부러 똑같이** 맞췄다 — 같은 칸에 갈아 끼우려고.
Autotag 의 수익 목표는 **$$$(최대 수익)** 으로 만들었다(오너가 "둘 다" 선택, 2026-09-02).

라이브러리: `//acscdn.com/script/aclib.js` — 문서당 한 번.

## 코드가 어떻게 도는가

1. `Layout.astro` — 프로덕션 + 한국어가 아니면 `</body>` 앞에 부트스트랩 스크립트를 넣는다.
2. 그 스크립트가 `/api/geo` 로 나라를 확인한다. 유럽이면 **거기서 끝**(스크립트를 안 받는다).
3. 아니면 `aclib.js` 를 붙이고, 로드되면 Autotag 를 돌리고
   페이지의 `[data-acs-zone]` 칸마다 배너를 실행한다.
4. `AdBanner.astro` 는 **빈 칸만** 그린다. 라벨("광고")은 기본 숨김이고,
   실제로 광고를 붙일 때만 `.on` 이 붙어 보인다 — 유럽에서 "광고" 글자만 남지 않게.

⚠️ 배너는 `document.createElement('script')` 로 그 칸 **안에** 넣는다.
aclib 이 `currentScript` 의 부모에 그리기 때문이다. `innerHTML` 로 넣으면 실행조차 안 된다.

## 🚨 가로 스크롤 함정 (2026-09-02 실제로 겪음)

**Autotag 의 인페이지푸시가 화면 밖 오른쪽에 `position:absolute` 요소를 미리 깔아 둔다.**
그대로 두면 문서 폭이 늘어난다 — 실측: 1700px 화면이 **3188px** 이 되고 800px 밀렸다.

`Layout.astro` 에 `html, body { overflow-x: clip }` 으로 막았다. 두 가지가 함정이다.

1. **`html` 만, 또는 `body` 만 걸면 안 먹는다.** 각각 계산값은 `clip` 인데 스크롤은 그대로였다.
   **둘 다** 걸어야 멈춘다.
2. **`hidden` 이 아니라 `clip`.** hidden 은 스크롤 컨테이너를 만들어 우측 레일(`.ad-rail`)과
   목차(`.article-side`)의 `position:sticky` 를 죽인다. 게다가 실측에서 넘침도 못 막았다.

## 광고 칸 위치

한국어는 각 페이지가 `AdFitBanner` 를 직접 갖고 있고, 해외는 `AdBanner` 를 같은 자리에 넣었다.

| 페이지 | 칸 |
|---|---|
| 기사(`[category]/[slug]`) | 요약 뒤 300×250 · 본문 뒤 300×250 · 우측 레일 160×600 |
| 목록·홈 | 300×250(모바일) · 728×90(PC) · 우측 레일 160×600 |

`.ad-slot-mobile`/`.ad-slot-pc`(1280px 경계)와 `.ad-rail` CSS 는 `Layout.astro` 전역에 있다.
한국어 페이지들은 각자 로컬 `<style>` 에 같은 것을 갖고 있다(원래 그랬다, 안 건드렸다).
