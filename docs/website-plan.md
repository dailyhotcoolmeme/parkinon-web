# parkinon.com 웹사이트 계획

> 확정일: 2026-08-01
> 이 문서는 **대화가 길어져 앞부분이 사라지는 일**을 막기 위해 만들었다.
> 실제로 한 번 날려서 오너가 같은 얘기를 두 번 하게 만들었다. 결정이 나올 때마다 여기에 적는다.

---

# 🔵 새 세션은 여기서부터

## 지금 어디까지 왔나

**화면 구성(와이어프레임) → 실제 Astro 코드 → dev 배포까지 끝났고, 배포 후에도 디자인
디테일을 계속 다듬는 중(진행형).**

- ✅ 목적·구조 확정
- ✅ 콘텐츠 4개 축 확정 (+ 의학 정보 2개 축은 **넣지 않기로** 확정)
- ✅ 한국용/해외용 나누는 원칙 확정
- ✅ 언어 범위 확정 (ko/en/fr/ja)
- ✅ 해외판 축 1(제도·지원) 스코프 확정
- ✅ 정적 생성 도구 확정 (Astro)
- ✅ 광고 상세 확정 (개수·배치, PC 세로 레일 포함)
- ✅ 화면 구성(레이아웃/와이어프레임) — 스크래치패드 목업으로 9개 페이지 전부 확정
- ✅ **실제 Astro 코드 이식 (2026-08-05)** — `parkinon-web/site/` 서브프로젝트.
  홈 + 3개 축(제도·지원/생활요령/파킨온소식) 허브·상세 + 도구 허브·복약시간표 도구,
  총 9개 라우트. 공유 컴포넌트(Header/Footer/AdBox), 이미지 `astro:assets` 자동 최적화,
  복약 시간표 도구 실동작(추가·삭제·인쇄) 확인 완료.
- ✅ **dev 배포 (2026-08-05, 이후 여러 차례 재배포)** — 아래 "배포" 섹션 참고. 정식 오픈
  (루트 도메인 전환)은 **콘텐츠가 꽤 쌓인 뒤**로 미루기로 함(오너 결정).
- ✅ **배포 후 디자인 디테일 다듬기 (2026-08-05~06)** — 아래 "코드/디자인 현재 상태"와
  "이 세션에서 확립된 디자인 규칙" 섹션 참고. 홈 + 허브 3개(제도지원/생활요령/파킨온소식)
  전부 히어로·빈공간 문제를 이 라운드에서 고쳤다.
- ✅ **상세페이지 3개 오른쪽 사이드 패널 (2026-08-06)** — 오너 승인안 = "목차 + 같은
  카테고리 다른 글". 상세 3개 전부에 사이드를 넣고 dev 재배포까지 완료. 이후 외부 조사로
  패널 디자인을 다시 만들고(카드 제거·현재 섹션 표시), 허브까지 같은 컴포넌트로 통일했다.
  이제 허브·상세 모두 넓은 화면에서 오른쪽이 비지 않고 좌우 폭도 같다.
- ⬜ 키워드 조사·용어집(실제 콘텐츠 작성)은 **미결** — 지금 있는 글은 카테고리당 1개씩
  뼈대 확인용 예시 글이고, 나머지 목록 항목은 아직 실제 글이 없어 목업처럼 `href="#"` 상태

## 코드/디자인 현재 상태 (2026-08-06 기준)

**라우트 (9개, 전부 `parkinon-site-dev`에 배포됨)**
```
/                                              홈
/institutions                                  제도·지원 허브
/institutions/copayment-reduction-guide        제도·지원 상세(예시 글 1개)
/lifestyle                                      생활 요령 허브
/lifestyle/walking-together-fall-prevention     생활 요령 상세(예시 글 1개)
/news                                           파킨온 소식 허브
/news/exercise-dopamine-neuron-protection       파킨온 소식 상세(예시 글 1개)
/tools                                          도구 허브 — 내비게이션에서 의도적으로 숨김
/tools/medication-schedule                      복약 시간표 도구(실동작) — 마찬가지로 숨김
```
나머지 허브 목록 항목들(각 허브에 10개 안팎)은 아직 실제 글이 없어 `href="#"`다.
지어내지 말고, 실제 글이 생기면 그때 slug를 붙여 연결할 것.

**오른쪽 사이드 패널 = `src/components/ArticleSide.astro` (허브 3개 + 상세 3개 공용)**

- 상세는 목차 + "OO 다른 글" 2블록, 허브는 "OO 다른 글" 1블록(`toc` 안 넘기면 자동으로 빠짐).
- 목록은 각 허브 목록에서 현재 글만 뺀 **실제 글 목록**이다. 지어낸 인기글 통계는 쓰지 않는다.
- 목차 링크는 본문 `h2`의 `id`를 가리킨다. 페이지 프론트매터 `toc`의 `id`·`label`이 본문 h2와
  **정확히 일치해야** 한다(한쪽만 고치면 링크가 죽는다). sticky 헤더에 제목이 가리지 않도록
  `.article-body h2{ scroll-margin-top: 88px }`. 부드러운 스크롤은 쓰지 않는다(즉시 이동).
- 폭: **허브와 상세가 같은 그리드를 쓴다** — Layout 전역 `.content-body`
  `grid-template-columns: minmax(0,760px) minmax(300px,1fr)`, `column-gap:44px`.
  본문은 760px까지, 남는 폭은 사이드가 가져간다(1648px 화면에서 사이드 428px) →
  오른쪽에 빈 공간이 안 남으면서 두 페이지의 좌우 폭이 정확히 같다.
  **1080px 이하에서 사이드를 감춘다**(감추는 규칙은 컴포넌트가 갖고 있다).
  ⚠️ 예전에 허브만 `1fr + 300`, 상세만 `760 + 300` 가운데 정렬이라 허브→상세로 넘어갈 때
  좌우가 64px씩 어긋났다(오너 지적 2026-08-06). **폭 정의를 페이지별로 다시 쓰지 말 것 —
  `.content-body` 한 군데만 고친다.** 상세는 `.content-body .detail-body` 두 클래스를
  같이 쓰고, `.detail-body` 는 행 배치(breadcrumb/article/사이드)만 담당한다.
- ⚠️ `@media`의 `display:none`은 반드시 `.article-side{display:flex}` **뒤에** 와야 한다.
  앞에 두면 뒤 규칙에 덮여 사이드가 안 사라진다(실제로 1000px에서 사이드가 남아 본문이
  눌렸다 — DOM 실측으로 잡았다).

**사이드 패널 디자인은 외부 조사 결과다 (2026-08-06). 임의로 되돌리지 말 것.**

| 근거(1차) | 확인한 내용 | 우리 적용 |
|---|---|---|
| NN/g "Table of Contents: The Ultimate Design Guide" | 우측 레일 목차에 박스/카드를 두르면 광고로 보여 무시된다(right-rail blindness). "simple, non-graphical design" 권장. 레일 목차는 sticky + **현재 섹션 하이라이트 강력 권장**. 링크 문구는 소제목과 정확히 일치시킬 것 | 회색 카드 제거(투명 배경), sticky 유지, 현재 섹션 표시 추가 |
| NHS 디자인시스템 Contents list(사용자 조사) | "The active link formatting helped users know where they were", 현재 항목에 `aria-current` | 현재 항목에 `aria-current="true"` + 초록 글자·왼쪽 초록 막대 |
| nhs.uk 파킨슨병 페이지 실측 | 목차 항목 19px, 카드 없음 | 목차 16px(본문과 동일. 기존 13.5px는 본문보다 작았다) |
| MDN 실측 | 컨테이너 배경 transparent·border 0, 링크 16px, `aria-current` | 위와 같음 |
| Wikipedia(2023 Vector) 실측 | 사이드 목차 배경 없음, 활성 항목 굵게 | 활성 항목 굵게(700) |

- **회색 카드를 다시 씌우지 말 것.** 바로 옆 `AdBox`가 회색 라운드 박스라, 카드를 씌우면
  광고 자리와 똑같이 보인다(위 right-rail blindness와 정확히 같은 실패).
- 대비 실측(WCAG AA 4.5:1 기준 통과): 라이트 목차 18.9 / 활성 5.13 / 날짜 5.74,
  다크 목차 16.3 / 활성 7.87 / 날짜 8.79.
- 패널 제목("이 글 순서" / "OO 다른 글") ↔ 첫 항목 간격은 **글자 기준 18px**로 맞춰 놨다.
  박스 여백이 아니라 글자 시작 위치로 맞춰야 두 패널이 같아 보인다(목차 링크는 padding
  7px + 행간 여유가 있어 같은 margin 을 주면 더 벌어진다) → `.toc-list` 6px / `.link-list` 13px.
  ⚠️ 이 margin 은 반드시 `.article-side .toc-list` 처럼 써야 한다. `.toc-list{margin-top:...}`
  로 쓰면 위의 `.article-side ul{margin:0}` (우선순위 0-1-1)에 밀려 **여백이 통째로 죽는다**
  — 실제로 이 때문에 제목과 목록이 붙어 있었다(오너 지적, 실측 글자간격 5px).
- 현재 섹션 판정은 컴포넌트 안의 스크롤 스크립트가 한다(화면 상단 120px 선을 마지막으로
  지나간 h2). **글 맨 위에서는 첫 항목이 선택 상태다** — 페이지에 들어오자마자 아무것도
  선택 안 된 채로 보이면 안 된다(오너 지시 2026-08-06). 항상 정확히 1개만 선택된다.

**주요 파일**
- `site/src/layouts/Layout.astro` — 디자인 토큰(`:root` 변수), 페이지 셸(`.layout`/`.ad-rail`/`main.col`), 자체 호스팅 Pretendard 폰트
- `site/src/components/Header.astro` / `Footer.astro` / `AdBox.astro` — 공유 컴포넌트(전부 실동작: 검색 패널·햄버거·사업자정보 토글)
- `site/src/assets/images/` — 로고·hero·path·food·yoga·sleep·family·lab.jpg + `appshots/`(앱스토어 실제 스크린샷 9장, 실제 App Store Connect에서 가져온 것)
- 각 페이지는 `astro:assets`의 `<Image>` 또는 `img.src`(배경 이미지용)로 이미지 최적화(WebP 자동 변환) 적용됨

**로컬 개발**
```bash
npm run dev --prefix site       # localhost:4321
npm run build --prefix site     # astro check + astro build (0 errors 유지할 것)
```
`.claude/launch.json`(parkinon-app 저장소 쪽에 있음, `site/`가 아니라 **메인 세션
작업 디렉토리 기준**)에 `parkinon-site`(포트 4321) 프리뷰 설정이 이미 있음.

## 이 세션에서 확립된 디자인 규칙 (반복 지적받은 것들, 꼭 지킬 것)

1. **넓은 PC 화면에서 텍스트를 억지로 여러 줄로 쪼개놓지 말 것.** 한 줄로 붙을 공간이
   있으면 붙여라. (홈 CTA 제목/문단, 푸터 면책문구 전부 이 이유로 다시 고쳤다.)
2. **넓은 화면에서 콘텐츠 폭을 좁게 고정해서 오른쪽에 빈 공간이 남는 걸 방치하지 말 것.**
   발견하면 `margin:0 auto`로 가운데 밀어넣는 정도로 때우지 마라 — "대충 감췄다"는
   지적을 받았다. **진짜 콘텐츠(사이드 패널, 관련 글 목록 등)로 채우는 걸 먼저
   고려할 것.** 허브·상세 모두 오른쪽을 "OO 다른 글"(+상세는 목차) 사이드 패널로 채웠다
   — 실제 같은 카테고리 글 목록이지 지어낸 "인기글" 통계가 아니다.
   구조는 `.content-body`(그리드) + `ArticleSide` 컴포넌트, 1080px 이하 자동 숨김.
   자세한 내용은 위 "오른쪽 사이드 패널" 섹션 참고.
3. **히어로 이미지 규칙이 페이지 종류별로 다르다.**
   - 홈: 화면 전체 폭(full-bleed, `.layout` 바깥, viewport 100%).
   - 허브 페이지(제도지원/생활요령/파킨온소식): **광고 레일 안쪽 "작업 영역"(`main.col`)
     에만 걸친다.** `main.col`의 padding을 negative margin으로 상쇄해서 그 안에서만
     edge-to-edge가 되게 구현(`.hub-hero-full{margin:-28px calc(-1 * var(--gutter)) ...}`).
     홈처럼 화면 전체로 뻗으면 안 된다는 걸 오너가 명확히 지적했다.
   - **허브 3개의 히어로 제목은 같은 높이에서 시작해야 한다**(오너 지적 2026-08-06).
     예전에는 히어로가 아래쪽 정렬(`align-items:flex-end`)이라 설명글이 3줄인 제도·지원만
     제목이 24px 위로 올라가 있었다. 지금은 **위쪽 정렬 + 고정 padding-top**(데스크톱 114px /
     640px 이하 58px)으로 못박아서, 설명이 몇 줄이든 제목 위치가 안 움직인다. 설명글 최대폭도
     640px로 맞춰 뒀다(실측: 560~719px 구간에서만 세 페이지가 모두 2줄. 500px이면 제도·지원만
     3줄, 720px이면 생활 요령만 1줄).
   - **히어로 높이도 3개가 같아야 한다** — 메뉴를 옮겨 다닐 때 높이가 들쭉날쭉하면 거슬린다는
     오너 지적(2026-08-06). 설명글 줄 수가 폭·페이지마다 2~4줄로 갈리므로 **설명 영역 높이를
     최대값으로 고정**했다: 데스크톱 `height:48px`(2줄), 640px 이하 `height:96px`(4줄) +
     `-webkit-line-clamp`. 히어로는 데스크톱 240px / 모바일 224px 로 고정된다.
     실측 확인(320·360·375·390·430·640·700·900·1300·1600px × 허브 3개): 모든 폭에서
     높이·제목 시작 위치가 세 페이지 동일, 잘리는 문구 없음.
     ⚠️ 설명 문구를 길게 고치면 잘릴 수 있다. 문구를 바꿀 때는 위 폭들에서 줄 수를 다시 재고,
     넘치면 문구를 줄이거나 이 높이 값을 같이 올릴 것(한 페이지만 올리면 다시 어긋난다).
   - 사진 위에 제목을 **오버레이**해서(어두운 그라디언트 스크림 + 흰 텍스트), 사진이
     제목보다 시각적으로 커 보이지 않게 할 것. 사진과 텍스트를 좌우로 나란히 배치하는
     방식은 "사진이 제목보다 커 보인다"는 이유로 반려됐다.
4. **데스크톱용 스타일이 모바일 레이아웃에 그대로 딸려오지 않는지 볼 것.** 헤더 메뉴의
   현재 항목 밑줄이 데스크톱용 `::after`(`bottom:-6px`) 그대로라, 모바일 햄버거 메뉴에서는
   초록 줄이 회색 구분선보다 4px 아래(다음 항목 영역)에 떠 있었다(오너 지적 2026-08-06).
   모바일에서는 `::after` 를 끄고 **행 구분선 자체를 초록으로** 바꿔 위치를 맞춘다.
5. **큰 시각적 변경은 제안 → 승인 → 구현 순서를 반드시 지킬 것.** "제안해달라"는
   요청에 바로 실제 코드/배포로 답하지 마라. 한 번 이걸 어겨서 크게 화를 냈다
   ("바로 하지 말고 나한테 검사 받고 해"). 방향을 말로 먼저 제시하고, 승인되면 그때
   코드로 만들 것. 다만 사용자가 이미 구체적 방향을 지정했으면(예: "오버레이로 해봐")
   그건 승인된 지시이니 바로 구현해도 된다 — 매번 다시 물을 필요는 없다.
6. **스크린샷 검증 도구(Claude Browser pane, headless Chrome 캡처)가 이 세션 내내
   가끔 흰 화면/텍스트 잘림 등 캡처 아티팩트를 보였다.** 이상해 보이면 먼저
   `getBoundingClientRect`/`scrollWidth` 등 DOM 측정으로 실제 상태를 확인하고,
   "캐시 문제일 것"이라고 둘러대며 넘어가지 말 것 — 사용자가 그 핑계에 크게 화를 냈다.
   진짜 원인을 찾아서 구체적으로 설명할 것.
7. **딱 맞는 사진이 없는 카테고리(예: 제도·지원)는 톤이 비슷한 기존 사진으로 대체**하고
   그렇게 했다고 밝힐 것 — 없는 사진을 새로 지어내거나 억지로 끼워맞추지 않는다.
   (제도·지원 히어로는 family.jpg 재사용, 파킨온소식은 lab.jpg — 둘 다 이미 있던 사진.)

## 배포 (2026-08-05, 이후 여러 차례 갱신)

**dev 배포만 나갔다. 정식 오픈(루트 도메인 전환)은 아직 안 했다 — 콘텐츠 쌓이면 진행.**

- Cloudflare Pages 프로젝트를 기존 `parkinon-web`(라이브, `parkinon.com`)과 **완전히
  분리된 새 프로젝트** `parkinon-site-dev`로 만들었다. 기존 라이브 사이트의 빌드 설정·
  라우팅은 전혀 건드리지 않았다.
- URL: **https://parkinon-site-dev.pages.dev**
- 재배포: `npm run deploy:dev --prefix site` (astro check → build → wrangler pages deploy)
- 관찰(2026-08-06): 이 저장소 `main`에 푸시해도 라이브 `parkinon-web` Pages 프로젝트에
  새 빌드가 생기지 않았다. 최근 배포는 4일 전 `67a4ab1`이 마지막이고, 그 뒤 커밋
  (`ae5a693`, `71c1871`)은 배포 목록에 없다. 푸시 직후 `parkinon.com`·`/terms` 모두 200.
  깃 연동이 꺼진 것인지 단순 스킵인지는 확인하지 않았다 — **정식 오픈(루트 도메인 전환)
  전에 대시보드에서 이 프로젝트의 깃 연동 상태를 반드시 확인할 것.**
- 정식 오픈 때 필요한 작업(아직 안 함): 기존 React SPA(`/records` 등 내부 라우트)를 `/app`
  아래로 옮기고, 이 Astro 빌드를 `parkinon-web`(루트 도메인) 프로젝트로 전환. 앱 코드에
  `parkinon.com/terms`, `/admin` 등 하드코딩된 링크가 있어 그 작업은 별도로 신중하게
  다시 확인받고 진행할 것.

## 다음에 할 일

아래가 미결이다. **오너가 "한꺼번에 던지지 말고 하나씩 하자"고 명확히 요구했다.**
목록을 통째로 늘어놓고 고르라고 하지 말 것. 하나 꺼내서 그것만 매듭짓고 다음으로 간다.

| # | 미결 | 급한가 |
|---|---|---|
| 1 | ~~언어 범위~~ | ✅ 확정 (아래 "언어 범위" 섹션) |
| 2 | ~~해외판 축 1(제도·지원)~~ | ✅ 확정 (아래 "해외판 축 1" 섹션) |
| 3 | ~~광고 상세~~ | ✅ 확정 (아래 "광고 상세" 섹션) |
| 4 | ~~정적 생성 도구~~ | ✅ 확정 (아래 "정적 생성 도구" 섹션) |
| 5 | **키워드 조사·용어집** | 콘텐츠 쓰기 직전 |
| 6 | 앱 유도 방안 | **콘텐츠 다 된 뒤에** 하기로 함 |
| 7 | ~~상세페이지 3개 오른쪽 빈공간 → 사이드 패널~~ | ✅ 완료 (2026-08-06, dev 배포까지) |

**바로 다음 할 일 = 5번, 키워드 조사·용어집(실제 콘텐츠 목록 뽑기).** 화면·코드·
dev 배포까지 끝났으니, 그다음은 실제로 무슨 글을 쓸지 정하는 단계다. 지금 있는 글은
카테고리당 1개씩 뼈대 확인용 예시라, 허브·사이드 목록의 나머지 항목은 전부 `href="#"`다.

## 정적 생성 도구 (확정, 2026-08-01)

**Astro.**

외부 조사 결과 근거:
- 콘텐츠 중심 사이트에서 Astro는 JS를 훨씬 적게 보내 Core Web Vitals·검색 순위에 유리
  (실측 비교 사례: 9KB vs Next.js 463KB) — 검색 유입이 목적인 이 사이트와 정확히 맞음
- **다국어 라우팅이 기본 내장**(Astro 4.0+, `astro.config.mjs` 로케일 설정 + `/[locale]/`
  폴더 구조) — ko/en/fr/ja 구조에 바로 맞음
  - ⚠️ **hreflang 태그는 자동 생성이 아니라 직접 구현해야 함**(이미 계획에 있던 "다국어
    URL 구조 + hreflang 미리 잡아둔다" 항목과 연결 — 별도 작업 필요, 새로운 이슈 아님)
- **기존 앱(`/records/*`)과 공존**하는 구조(콘텐츠는 Astro, 인증 필요한 부분은 기존 React
  그대로)가 2026년 실제로 흔히 쓰이는 하이브리드 패턴 — 지금 계획과 일치
- Cloudflare Pages 공식 배포 가이드 있음. 단 `/records/*`(기존 SPA)와 Astro 콘텐츠 페이지가
  안 겹치게 `_routes.json` 등 라우팅 설정을 별도로 신경 써야 함(설계 단계 작업 항목)

---

## 이 세션에서 오너가 지적한 것 (반복하지 말 것)

- **한꺼번에 던지지 마라.** "내용도 결정도 무조건 하나씩." 여러 결정을 한 번에 물으면
  오너가 결정을 못 한다.
- **앞서가지 마라.** 정한 적 없는 것을 정해진 것처럼 말하지 마라. 실제로 계획에 없던
  "콘텐츠 덩어리 1~6번"을 지어내 보고했다가 "누가 정한 건데"라는 지적을 받았다.
- **기억에 의존하지 마라.** 합의 내용은 이 문서를 읽어라. 없으면 지어내지 말고 물어라.

## 앱 쪽 상태 (웹과 별개, 참고용)

- 스토어: 안드로이드 1.0.3 vc6 / iOS 1.0.3 빌드9 **심사 대기**. 새로 빌드할 것 없음
- OTA: 3개 런타임(1.0.1·1.0.2·1.0.3) 모두 최신 배포 완료
- 약관·개인정보: 한국어판·해외판 모두 정리·배포 완료
- 녹음 6개월 자동 삭제: **미구현**. 착수 조건은 앱 저장소 `CLAUDE.md` 맨 위 참조
- 응답 지연 1~2초: 오너가 보류하기로 함

---

---

## 목적

검색으로 들어온 사람을 앱으로 보낸다. 동시에 광고 수익을 만든다.

앱은 홍보 수단이 마땅치 않다. 프랑스어·일본어까지 번역해 놓고도 알릴 길이 막막했다.
웹에서 SEO 로 사람을 모아 앱으로 흘리는 것이 이 사이트의 존재 이유다.

---

## 구조

```
parkinon.com/          → 콘텐츠 (주력, 검색 유입)
parkinon.com/app/…     → 기존 기록보기 (서브로 이전)
```

`/about` `/terms` `/privacy` `/delete-account` `/records` 는 **앱에서 눌러 넘어오는 진입점**이다.
콘텐츠 논의 대상이 아니다. 건드리지 않는다.

---

## 콘텐츠 — 4개 축 (확정)

| 축 | 내용 | 역할 |
|---|---|---|
| **1. 제도·지원** | 산정특례, 뇌병변 장애등록, 장기요양등급, 의료비·세금 공제, 보조기기 지원 | **SEO 본체** |
| **2. 생활 요령** | 아래 확장 목록 참조 | 검색 + 해외 확장 |
| **3. 도구** | 복약 시간표 만들기, 진료 전 질문 체크리스트, 기록 서식 | 재방문·앱 전환 |
| **4. 파킨온 소식** | 뉴스·연구 요약 | 양념 |

축 1 은 경쟁이 비어 있다. `파킨슨 산정특례` 검색 시 **10년 된 개인 블로그가 2위**였다.

### 축 2 확장 목록

해외 3대 파킨슨 사이트(Parkinson's UK · Parkinson's Foundation · 마이클 J. 폭스 재단)
목차를 뜯어서 확인한 주제들이다. 새 축이 아니라 축 2 의 하위 주제로 넣는다.

- 약 시간 놓쳤을 때 / 식사 / 변비 / 낙상
- **진단 직후 첫걸음** — 해외 3사 모두 별도 대분류로 뺄 만큼 비중이 크다
- **마음 건강** (우울·불안·수면) — 위와 같음
- 일·직장 (젊은 발병 환자)
- 여행·이동
- 가족 관계
- 보조기기·기술
- 혼자 사는 환자
- 보호자 번아웃

---

## ⛔ 의학 정보는 다루지 않는다 (확정)

해외 사이트에 있고 우리에게 없는 축이 **둘** 있다. 조사해서 확인했고, **의도적으로 뺀다.**

- **질환 이해** — 증상, 원인, 진단, 유전, 감별진단
- **약과 치료** — 약 종류, 부작용, 복용 원칙, DBS 수술, 재활

### 왜 빼는가

1. **자리가 이미 없다** — `파킨슨병 약물치료` 검색 시 서울대·삼성서울·서울아산병원이 상위를
   독점한다. 신규 도메인이 병원을 밀어내는 것은 사실상 불가능하다.
2. **써도 안 읽힌다** — 구글은 의료 정보에 작성자 자격을 본다. 자격 표기가 없으면 순위가
   안 나온다. 품은 들고 유입은 없다.
3. **틀리면 사람이 다친다** — 약 용량·부작용·병용금기가 특히 그렇다. 우리 사용자는 60대
   이상이고 그대로 따라 할 수 있다.
4. **앱까지 같이 깎인다** — 웹에서 의학 정보가 틀리면 앱 신뢰도로 번진다. 앱이 본체다.

### 근거 — 해외 사이트는 전부 의사가 한다

**Parkinson's UK**: 파킨슨 전문 간호사 · 신경과 의사 · 노인의학 전문의 · 약사 ·
물리치료사 · 작업치료사 · 언어치료사 · 재무/법률 자문이 검수한다. **PIF TICK**(영국 유일
보건정보 품질 인증) 을 받고 제작 과정을 외부 감사받는다. 환자와 공동 제작하고 읽기 연령을
정기 측정한다. 그리고 **"AI 도구로 건강 정보를 쓰지 않는다. AI 를 의학적 근거의 출처로
절대 쓰지 않는다"** 고 명시해 뒀다.

Parkinson's Foundation · 마이클 J. 폭스 재단도 재단 소속 의료진이 있다.

**신경과 의사 섭외는 불가능하다는 것이 오너 판단이다(2026-08-01).**

### 참고 — 의사 없이 하는 곳의 방식

`parkinsons.org` 는 재단이 아닌 독립 운영자인데 의학 정보를 낸다. 이렇게 한다.

> 우리 콘텐츠는 아직 의사의 검수를 받지 않았다. 의료자문단이 구성되기 전까지 어떤
> 페이지에도 "검수: ○○○" 표기를 달지 않는다. 전문가 자격을 지어내는 것보다 이렇게
> 밝히는 것이 정직하다고 믿는다.

「우리가 아닌 것」 페이지를 따로 두고, 모든 주장을 NINDS·MDS 지침·Cochrane 에 각주로
걸고, 모든 글에 최종 수정일을 붙이고, 오류 신고는 72시간 내 고친다.

**다만 이 도메인은 2002년 등록이다.** 24년 쌓인 신뢰를 업고 하는 것이라 우리에게 그대로
적용되지 않는다. 나중에 의사를 구하게 되면 이 방식을 참고한다.

---

## 한국용 / 해외용 (확정)

**원칙: 가능하면 하나로 쓰고 번역한다. 나라별로 갈라야만 하는 것만 가른다.**

| 축 | 나라별 따로 | 하나 쓰고 번역 |
|---|---|---|
| 1. 제도·지원 | ⭕ **나라마다 완전히 다름** | ✕ |
| 2. 생활 요령 | ✕ | ⭕ |
| 3. 도구 | ✕ | ⭕ |
| 4. 파킨온 소식 | ✕ | ⭕ |

**제도·지원만 나라별이다.** 그런데 이것이 SEO 본체라 해외에서는 축 1 을 그대로 쓸 수 없다.
해외 대상국(미국·일본·캐나다·호주·뉴질랜드) 제도를 각각 조사해야 하므로 부담이 크다.
→ **해외판 축 1 을 어떻게 할지는 아직 안 정했다.**

---

## 언어 범위 (확정, 2026-08-01)

**ko/en/fr/ja 4개 언어 유지.**

앱 정적 페이지(`/about` `/terms` `/privacy` `/delete-account`)가 이미 이 4개 언어로
번역되어 있다. 웹사이트도 그대로 맞춰서 관리 포인트를 하나로 유지한다.

해외 배포 대상 6개국(한국·미국·일본·캐나다·호주·뉴질랜드) 기준으로 보면 프랑스어권은
없지만(캐나다는 영어권 비중이 큼), 이미 번역이 있는 언어를 새로 줄일 이유가 없다는 판단.

---

## 해외판 축 1(제도·지원) 스코프 (확정, 2026-08-01)

**5개국 다 한다. 단, 캐나다는 연방 제도까지만 — 주(州)별 항목은 뺀다.**

착수 전 "조사가 가능한지 / 최신성이 유지되는지"를 먼저 확인했다. 5개국 공식 출처를
표본 조사한 결과:

| 나라 | 제도 구조 | 공식 출처 | 난이도 |
|---|---|---|---|
| 🇯🇵 일본 | 전국 단일 (難病医療費助成制度) | nanbyou.or.jp | 쉬움 |
| 🇺🇸 미국 | 연방 단일 (SSDI, SSA Blue Book 11.06) | SSA.gov | 쉬움 |
| 🇦🇺 호주 | 전국 단일 (NDIS, List B) | ndis.gov.au | 쉬움 |
| 🇳🇿 뉴질랜드 | 전국 단일 (Work and Income) | workandincome.govt.nz | 쉬움 |
| 🇨🇦 캐나다 | 세금공제는 연방 단일(CRA DTC), **의료비·요양지원은 주(10개)마다 따로** | canada.ca | 세금공제만 쉬움 |

일본 샘플 조사(호른-야르 중증도 기준, 본인부담 상한액, 신청 절차)로 실제 번역까지
해봤고 밀도·소요 시간이 나머지 3개국(미·호·뉴)과 비슷할 것으로 판단.

캐나다만 주별로 하면 사실상 5개국이 아니라 14개 관할(연방 + 주 10개 + 준주 3개)이 되어
버린다 → **캐나다는 연방 제도(장애 세금공제 DTC, CPP 장애연금)까지만 다루고 주별 의료비·
요양지원은 축 1에서 뺀다.** 이러면 5개국 조사 부담이 균등해진다.

**최신성 관리**: 정부 제도는 보통 연 1회 정도(급여액·기준 갱신)만 바뀐다. 모든 페이지에
"○년 ○월 기준" 표기 + 연 1회 재확인을 기본으로 하고, 필요하면 Firecrawl monitor 로 각국
공식 페이지(5~6개)를 워칭해서 변경 알림을 받는 방식도 가능(도입 여부는 나중 결정).

---

## 함께 확정된 것 (아침 논의)

### 글 형식

```
[파킨온 소식 #N] 제목

> 이 글은 최근 발표된 연구와 기사 내용을
> 파킨온이 쉬운 말로 정리해 다시 쓴 것입니다.

본문
**이렇게 나왔습니다**
**환자분께**
**보호자분께**
댓글 유도 한 줄

⚠️ 면책 문구 (자동 삽입)
📰 출처
```

### 콘텐츠 원칙

- **기사 복사·번역 금지.** 사실만 재료로 쓰고 문장은 새로 쓴다
- **사진은 우리가 만든 이미지**를 종류별로 돌려 쓴다 (기사 사진 금지)
- 보조제·민간요법 소개 금지 (애드센스 정책)
- 제도 정보는 공식 출처 기준 + "○년 ○월 기준" 표기

### 발행

주 3회(월·수·금) · AI 초안 → 관리자 웹에서 검토·수정 → 발행

### 디자인

앱 `DESIGN_SYSTEM.md` 유지 — 큰 글씨, 고대비, 큰 버튼

**색상 (확정, 2026-08-03)**: 배경은 흰색(`#ffffff`). 컬러는 앱 실제 브랜드 컬러 그대로 재사용 —
그린 `#4CAF50`(진한 톤 `#2E7D32`), 오렌지 `#FF9800`(앱 `DESIGN_SYSTEM.md` 참조).
초안 단계에서 임의로 다른 팔레트(어두운 그린 `#012d1d`, 파스텔 오렌지 `#ea9147`, 크림색 배경
`#fbf9f4`)를 새로 만들었다가 "어설프다"는 지적을 받고 앱 브랜드 컬러로 되돌렸다 — 웹 전용
색을 새로 짓지 말 것.

**홈 히어로 (확정, 2026-08-03)**: 검색으로 처음 들어온 방문자는 "파킨온"을 모른다는 전제로
4가지 방향(효용 약속형/콘텐츠 우선형/검색의도 질문형/공감형)을 비교했고, **1안(효용 약속형)**
으로 확정. "파킨슨병과 함께하는 하루하루, 조금 더 수월하게" — 브랜드 소개가 아니라 방문자가
얻는 것을 헤드라인으로 삼는다. 앱 다운로드 CTA는 페이지 하단에 이미 있으므로 히어로에서
중복하지 않는다.
⚠️ **오너가 "나중에 히어로 자체를 뺄 수도 있다"고 여지를 남겼다** — 콘텐츠가 쌓이고 트래픽이
보이면 2안(콘텐츠 우선형, 히어로를 한 줄로 줄이고 바로 "오늘의 소식"으로 진입)으로 전환할
수 있음. 지금 1안으로 확정했다고 콘텐츠 우선형을 완전히 폐기한 것으로 오해하지 말 것.

### 기술

- **정적 HTML 생성** — 지금 SPA 는 검색에 안 잡힌다
- 다국어 URL 구조 + hreflang 을 **미리** 잡아둔다 (나중에 바꾸면 순위를 잃는다)
- 쿠키 동의 + 구글 인증 CMP 를 **처음부터** 넣는다

### 광고

- 넣는다. **한국 포함** (앱과 다르다 — 앱은 한국에 광고가 없다)
- 자리는 설계 때 확보하고 **밀도·배치는 오너가 결정한다**
- 애드센스는 글이 쌓인 뒤 신청한다
- 건강 콘텐츠 제한 없음 (구글 게시자 제한 목록 직접 확인함)
- **⚠️ 정적 생성 도구로 템플릿(레이아웃) 잡을 때, 광고 자리는 필수로 확보해둘 것
  (2026-08-01 확정).** 개수·밀도·정확한 배치 같은 세부는 나중(콘텐츠 쌓이고 트래픽 보면서)
  결정해도 되지만, 템플릿 자체에 광고용 컨테이너가 없으면 나중에 레이아웃을 다시 손봐야
  한다. 그러니 템플릿 설계 단계에서 "여기에 광고가 들어간다"는 자리만은 빼먹지 말 것.

#### 광고 상세 (개수·배치, 2026-08-01)

**모바일/일반 레이아웃**
| 페이지 | 위치 | 개수 |
|---|---|---|
| 글 목록 | 6~8개마다 네이티브(글처럼 보이는) 광고 1개 | 목록당 2~3개 |
| 글 상세 | 본문 시작 직후 + 본문 중간(긴 글만) + 본문 끝(공유/댓글 앞) | 최대 3개 |
| 홈/제도·지원 허브 | 하단 1개 | 1개 |

- 형식: 반응형(Responsive) 디스플레이 기본. 스크롤 따라다니는 sticky는 모바일에서 안 씀
  (콘텐츠 가림 + 어르신 타겟엔 방해 요소가 큼)
- 오클릭 방지: 광고 주변 최소 24px 여백 + "광고" 라벨 항상 표시 + 앱 이동 CTA와는 화면상
  최대한 멀리 배치

**PC 레이아웃 — 세로 레일 광고 (toolshere `app/Shell.tsx` 패턴 그대로 채용)**

toolshere(아워마인의 다른 사이트)에 이미 검증된 반응형 세로 레일 구조가 있어 그대로 가져온다.

- 본문 작업 영역 최대 폭: **1280px**
- 뷰포트 **≥1648px**: 컨테이너 바깥 좌·우 양쪽에 세로 레일 광고, 각 **160px** 폭
- 뷰포트 **1280~1647px**: 본문 폭을 줄여서 우측에만 세로 레일 광고 **300px** 폭
- 뷰포트 **<1280px**: 레일 광고 없음(본문 풀폭) — 이 아래부터는 모바일 레이아웃 규칙 적용
- 전부 `sticky top-20`(스크롤해도 계속 보임)
- 참고 구현: `/Users/ourmine/dev/toolshere/app/Shell.tsx:321-532`,
  `/Users/ourmine/dev/toolshere/app/components/AdSlot.tsx`,
  `tailwind.config.ts`의 `container-max: 1280px`

### 회원가입

없다. 앱으로 보내는 것이 목적이다.

### 유럽 대응

**웹사이트는 대리인이 필요 없다. 다국어여도 마찬가지다.**
회원가입이 없고 방문자에게서 아무 정보도 받지 않는다. 처리하는 개인정보는 광고·분석
쿠키뿐이다. 필요한 것은 쿠키 동의와 개인정보처리방침이다.

⚠️ **앱 기준을 웹에 가져다 붙이지 말 것.** 앱은 건강정보를 매일 수집해서 기준이 다르다.
실제로 이걸 섞어서 말이 계속 바뀌었고 오너가 크게 지적했다(2026-08-01).

---

## 아직 안 정한 것

1. **광고 상세** — 개수·배치
2. **정적 생성 도구** — Astro 등
3. **키워드 조사·용어집** — 실제 콘텐츠 목록 뽑는 작업
4. **앱 유도 방안** — 콘텐츠가 다 구성된 뒤에 찾기로 함
