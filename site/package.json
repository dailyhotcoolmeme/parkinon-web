{
  "name": "parkinon-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build && node scripts/check-built-html.mjs && node scripts/check-i18n.mjs",
    "preview": "astro preview",
    "deploy:dev": "astro check && astro build && node scripts/check-built-html.mjs && node scripts/check-i18n.mjs && wrangler pages deploy dist --project-name=parkinon-site-dev --commit-dirty=true",
    "check:pseudo": "PSEUDO_I18N=1 astro build && echo \"가짜 언어 빌드 완료 — dist 를 열어 레이아웃을 볼 것\""
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.5",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.1.6"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^5.9.3"
  }
}
