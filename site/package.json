{
  "name": "parkinon-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build && node scripts/check-built-html.mjs",
    "preview": "astro preview",
    "deploy:dev": "astro check && astro build && node scripts/check-built-html.mjs && wrangler pages deploy dist --project-name=parkinon-site-dev --commit-dirty=true"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.5",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.1.6"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^5.9.3"
  }
}
