---
/*
 * 운동 영상 허브 (할 일 0-5, 2026-08-07).
 *
 * 다른 허브(소식·생활 요령·제도)와 달리 **글이 아니라 영상 목록**이다. 데이터는
 * `src/lib/exerciseVideos.ko.ts` — 출처·검증 근거는 그 파일 주석 참고.
 *
 * ⚠️ 영상은 <iframe> 으로 심지 않는다. **썸네일(정적 이미지) + 새 탭 링크**로만 연결한다.
 *   지금 이 사이트는 "쿠키 0개·분석/광고 스크립트 0개"가 실측된 상태이고
 *   (site-implementation.md "쿠키 동의·CMP"), YouTube iframe 을 심으면 그 상태가
 *   조용히 깨진다. Google 인증 CMP 를 켤 때(1-8) 같이 정리한다.
 *
 * 난이도 탭은 밑줄 방식이다(오너 확정 2026-08-08: "필터칩 스타일이 틀린거라고
 * 얘기했잖아 — 알약 칩은 웹사이트에 안 쓴다"). 앱 전역 표준(VideoList 알약 칩,
 * memory project_parkinon_filter_chip_standard)은 **앱 전용**이라 이 웹사이트에는
 * 안 옮긴다 — 헤더 메뉴·임상시험 국가 탭과 같은 밑줄 방식으로 통일한다.
 */
import { getRelativeLocaleUrl } from 'astro:i18n';
import Layout from '../../../layouts/Layout.astro';
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';
import AdBox from '../../../components/AdBox.astro';
import yogaImg from '../../../assets/images/yoga.jpg';
import { VIDEO_DATA, DIFFICULTIES } from '../../../lib/exerciseVideos.ko';
import { EXERCISE_SOURCE } from '../../../lib/exerciseSource.ko';
import ExerciseSourceNote from '../../../components/ExerciseSourceNote.astro';
import Tr from '../../../i18n/Tr.astro';

const locale = Astro.currentLocale ?? 'ko';

const allVideos = DIFFICULTIES.flatMap((d) => VIDEO_DATA[d].map((v) => ({ ...v, difficulty: d })));
const youtubeUrl = (id: string) => `https://www.youtube.com/watch?v=${id}`;
const thumbUrl = (id: string) => `https://img.youtube.com/vi/${id}/hqdefault.jpg`;
---

<Layout
  title="운동 영상 — 파킨온"
  breadcrumbs={[
    { name: '홈', path: '/ko/' },
    { name: '운동 영상', path: '/ko/exercise/' },
  ]}
>
  <Header active="exercise" />

  <div class="layout">
    <aside class="ad-rail left">
      <AdBox minHeight="600px" />
    </aside>

    <main class="col">
      <section class="hub-hero-full" style={`background-image:url(${yogaImg.src});`}>
        <div class="hub-hero-full-inner">
          <h1>운동 영상</h1>
          {/* 히어로 문구는 어느 나라 페이지든 그대로 쓰는 한 줄이다(오너 확정 2026-08-07).
              "공식 검증"·기관명 얘기는 여기서 하지 않는다 — 그 얘기는 아래 출처 문단에만 있다.
              그래서 사전(src/i18n)의 exercise.tagline 하나로 관리하고 나라마다 새로 쓰지 않는다. */}
          <p><Tr k="exercise.tagline" /></p>
        </div>
      </section>

      <nav class="breadcrumb">
        <a href={getRelativeLocaleUrl(locale, '')}>홈</a><span class="sep">/</span><span>운동 영상</span>
      </nav>

      <ExerciseSourceNote source={EXERCISE_SOURCE} />

      <div class="hub-tabs" id="diffTabs">
        <button class="tab active" data-diff="전체">전체</button>
        {DIFFICULTIES.map((d) => <button class="tab" data-diff={d}>{d}</button>)}
      </div>

      <div class="video-grid" id="videoGrid">
        {
          allVideos.map((v) => (
            <a
              class="video-card"
              data-diff={v.difficulty}
              href={youtubeUrl(v.videoId)}
              target="_blank"
              rel="noopener"
            >
              <span class="thumb">
                <img src={thumbUrl(v.videoId)} alt="" loading="lazy" width="336" height="188" />
                <span class="duration">{v.duration}</span>
                <span class="play" aria-hidden="true">
                  <svg viewBox="0 0 24 24" fill="currentColor"><path d="M8 5v14l11-7z" /></svg>
                </span>
              </span>
              <span class="badge">{v.difficulty}</span>
              <h3>{v.title}</h3>
              <p>{v.description}</p>
            </a>
          ))
        }
      </div>
    </main>

    <aside class="ad-rail right">
      <AdBox minHeight="600px" />
    </aside>
  </div>

  <Footer />
</Layout>

<style>
  .breadcrumb {
    font-size: 13px;
    color: var(--text-sub);
    margin-bottom: 18px;
  }
  .breadcrumb a:hover {
    color: var(--primary);
  }
  .breadcrumb .sep {
    margin: 0 6px;
    opacity: 0.6;
  }

  /*
   * 필터탭 = 밑줄 방식(헤더 메뉴·임상시험 국가 탭과 같은 방식, 오너 확정 2026-08-08).
   * 알약 칩(앱 전용 project_parkinon_filter_chip_standard)은 이 웹사이트에는 안 쓴다.
   */
  .hub-tabs {
    display: flex;
    flex-wrap: nowrap;
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
    gap: 22px;
    margin: 22px 0 30px;
    padding-bottom: 14px;
    border-bottom: 1px solid var(--outline);
  }
  .hub-tabs::-webkit-scrollbar {
    display: none;
  }
  .tab {
    flex-shrink: 0;
    position: relative;
    font: inherit;
    background: none;
    border: none;
    padding: 0;
    cursor: pointer;
    font-size: 15.5px;
    font-weight: 700;
    color: var(--text-sub);
    white-space: nowrap;
  }
  .tab.active {
    color: var(--primary);
  }
  .tab.active::after {
    content: '';
    position: absolute;
    left: 0;
    right: 0;
    bottom: -17px;
    height: 2px;
    background: var(--primary);
  }

  .video-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
    /* 세로 간격만 22→40px 로 키웠다 — 카드 아래 두 줄 설명이 다음 줄 썸네일과 거의 붙어
       보였다는 지적(2026-08-07). 가로 간격은 그대로 둔다. */
    gap: 40px 18px;
    padding-bottom: 8px;
  }
  .video-card {
    display: block;
    color: var(--text);
  }
  .thumb {
    position: relative;
    display: block;
    border-radius: 10px;
    overflow: hidden;
    background: var(--surface-low);
    aspect-ratio: 16 / 9;
  }
  .thumb img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }
  .thumb .duration {
    position: absolute;
    right: 6px;
    bottom: 6px;
    background: rgba(0, 0, 0, 0.75);
    color: #fff;
    font-size: 11.5px;
    font-weight: 700;
    padding: 2px 6px;
    border-radius: 4px;
  }
  .thumb .play {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #fff;
    opacity: 0.92;
  }
  .thumb .play svg {
    width: 46px;
    height: 46px;
    filter: drop-shadow(0 2px 6px rgba(0, 0, 0, 0.5));
  }
  .video-card .badge {
    display: inline-block;
    margin-top: 10px;
    font-size: 11px;
    font-weight: 800;
    color: var(--primary);
    text-transform: uppercase;
    letter-spacing: 0.04em;
  }
  .video-card h3 {
    font-size: 16px;
    margin-top: 4px;
    line-height: 1.4;
    /* 제목 길이가 들쭉날쭉해도 카드 높이를 맞춘다 */
    display: -webkit-box;
    -webkit-box-orient: vertical;
    -webkit-line-clamp: 2;
    overflow: hidden;
  }
  .video-card p {
    margin-top: 6px;
    font-size: 13px;
    line-height: 1.6;
    color: var(--text-sub);
    display: -webkit-box;
    -webkit-box-orient: vertical;
    -webkit-line-clamp: 2;
    overflow: hidden;
  }
  .video-card[hidden] {
    display: none;
  }

  @media (max-width: 480px) {
    .video-grid {
      grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
      gap: 32px 12px;
    }
  }
</style>

<script>
  // 순수 필터링 — 서버 재요청 없이 카드를 보이고 숨긴다. 프레임워크 없이 32개 정도는 이걸로 충분하다.
  const tabs = document.querySelectorAll<HTMLButtonElement>('#diffTabs .tab');
  const cards = document.querySelectorAll<HTMLAnchorElement>('#videoGrid .video-card');

  tabs.forEach((tab) => {
    tab.addEventListener('click', () => {
      tabs.forEach((t) => t.classList.remove('active'));
      tab.classList.add('active');
      const diff = tab.dataset.diff;
      cards.forEach((card) => {
        card.hidden = diff !== '전체' && card.dataset.diff !== diff;
      });
    });
  });
</script>
